#include<stdio.h>
int main()
{
	int year;
	printf("enter an year");
	scanf("%d",&year);
	if(year%4==0)
	{
		printf("the entered year is leap year");
	}
	else
	{
		printf("the entered year is not a leap year");
	}
	return 0;
}
