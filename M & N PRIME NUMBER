#include<stdio.h>
int main()
{
	int i,m,n,num;
	printf("enter the number between m and n");
	scanf("%d%d",&m,&n);
	for (num=m;num<=n;num++)
	{
		for(i=2;i<=n;i++)
	{
	if(num%i==0);
	break;
}
if(i==num)
printf("%d",num);
}
}
